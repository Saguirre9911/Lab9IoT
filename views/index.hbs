<h1>{{ title }}</h1>
<p>Welcome to Cómo es que se llama? {{ title }}</p>
{{# if condition }}
    Condition is met
{{ else }}
   Condition not met
{{/if}}

{{# each anyArray as |val key|}}
    {{ key }}: {{ val }}
{{/each}}